# k8s-configmap-secret-demo
A simple Flask application demonstrating configuration management in Kubernetes using ConfigMaps and Secrets.  The application reads non-sensitive configuration from a ConfigMap and sensitive values from a Secret, while also implementing resource constraints and health checks following Kubernetes best practices.

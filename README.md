# k8s-configmap-secret-demo
This project demonstrates Kubernetes configuration management using ConfigMaps and Secrets. The application runs as a Deployment with two replicas, consumes configuration from a ConfigMap, sensitive data from a Secret, exposes health endpoints for liveness and readiness probes, and uses resource requests and limits to ensure predictable scheduling and resource consumption.
